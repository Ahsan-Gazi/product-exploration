import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h3>What is Context Api?</h3>
            <p>Context API is a new feature which allow the user to share state accross the entire app</p>
            <p>Context API is a kind of react structure that enables to interchange unique details<br/> 
            and helps to solve prop-drilling problem</p>
            <p>Context API effectively produces global variable that can be passed around </p>
            <p>Context API helps to move props from grandparent to child to parent</p>
            
            <p>React.createContext() returns a Consumer and a Provider</p>
            <p> Provider is a component that as it's names suggests provides the state to its children</p>
            <br/><h3>What is semantic tag?</h3>
            <p>Semantics tags provide meaning to an HTML page rather than just presentation</p>
            <p>Semantic tag makes page more comprehensible to define different sections and layout of web pages</p>
            <p>It can cleary communicate with the developer and the browser</p>
            <p>The semantic tags help the search engines and other 
                user devices to determine the importance and context of web pages</p>
                <p>Semantic tags determine where texts will show and directed its format</p>
                <p>Specially semantic tags are kept inside angle brackets</p>
            <p></p>
            <br/><h3>Inline block vs inline block element</h3>
        </div>
    );
};

export default Blogs;